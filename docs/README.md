# docs

- `company/` — universal principles, loaded everywhere.
- `architecture/` — per-component design docs, loaded by their scope.
- `agents/` — routing map and committed config snapshots (branch protection JSON, test log).
